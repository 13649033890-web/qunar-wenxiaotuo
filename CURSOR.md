# 去哪儿「问小驼」原型：Cursor 编辑说明

这是一个可直接在 Cursor 中打开的 React + TypeScript 交互原型。

## 开始编辑

1. 解压项目并在 Cursor 中打开项目根目录。
2. 使用 Node.js 22.13 或更高版本。
3. 执行 `npm install`。
4. 执行 `npm run dev` 启动本地页面。

## 主要文件

- `app/page.tsx`：全部页面状态、点击流程和交互逻辑。
- `app/globals.css`：iPhone 17 画布、首页入口和 AI 页面样式。
- `public/reference/`：用户提供的去哪儿原页面截图，未改区域以这些截图为视觉基准。
- `public/reference/qunar-haidian-sug.jpg`：预置“海淀区酒店”的原 Sug 页面。
- `public/reference/qunar-haidian-hotels.jpg`：海淀区酒店原落地页。
- `public/brand/qunar-camel.png`：完整骆驼 Logo 独立资源。

## 当前产品约束

- 手机画布固定为 393 × 852，不随桌面评审导航缩放。
- 去哪儿原页面未涉及部分保持截图原样，不重新绘制。
- 首页不保留“足迹收藏”或独立“问小驼”按钮，搜索框横向拉长并保留“搜索”按钮。
- 点击首页搜索框默认进入“搜索”Tab，页面使用 `public/reference/qunar-current-search.png`，不重绘中间页内容。
- 顶部“搜索 / 问小驼”Tab负责两种页面切换；中间页搜索框不再承担AI跳转，中间页“搜索”按钮进入传统业务落地页。
- “问小驼”承接规划、住宿推荐和比较决策；三个功能页顶部返回均回到“问小驼”主界面。
- 离开功能页时清空思考/结果临时状态，二次进入后必须重新触发生成或搜索。
- 做攻略内包含“做攻略”和“抄攻略（外部链接导入）”。
- Logo 必须使用独立图片和 `object-fit: contain`，禁止重新使用整张截图的背景坐标裁切。
- 普通搜索点击“搜索”或 Sug 第一条业务线入口后，进入海淀区酒店落地页。
- 落地页浏览第 4 家不同酒店，或停留满 10 秒后，左侧显示“问小驼·比一比”浮动入口。
- 点击“比一比”必须复用现有酒店选择和对比流程，并带入海淀区酒店数据。

完整产品逻辑和老板后续方向见 [`HANDOFF.md`](./HANDOFF.md)。

## 交付前检查

- 首页搜索框完整覆盖原搜索区域，且没有“足迹收藏”和独立 AI 入口。
- 搜索中间页视觉与用户原图一致，搜索框可进入“问小驼”。
- 逐一验证三类功能返回“问小驼”，并验证二次进入不显示上次思考过程。
- 首页和 AI 页面中的骆驼 Logo 都完整显示。
- 左侧流程栏展开或收起时，手机内容仍为 393 × 852。
- 逐一点击首页、问小驼、做攻略、抄攻略、订票、找酒店、酒店对比。
- 分别验证酒店落地页的“第 4 家酒店点击触发”和“10 秒停留触发”。
